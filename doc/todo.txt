In order of priority:

Catch errors such as 404s

Fix nil responses.

Encode colons in URLs

Add jquery requests to get new images

mod jquery requests to get images without interrupting slide show

hack urls/permalinks to match jquery requests

Set up array structure so images can be pulled from slideshow without stopping flow

